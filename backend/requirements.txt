# Backend dependencies
# Pinned versions that have Python 3.14 wheels on PyPI
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
aiosqlite>=0.20.0
# psycopg2 is only needed for sync DB access (seed scripts, evaluation).
# On Windows, use the pre-built wheel via:
#   pip install psycopg2-binary --only-binary :all:
# On Linux/Mac:
psycopg2-binary; platform_system != "Windows"
alembic>=1.13.1
pydantic>=2.7.1
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
joblib>=1.4.2
numpy>=1.26.0           # 2.x compatible with Python 3.14
pandas>=2.2.0
scikit-learn>=1.5.0
xgboost>=2.0.3
httpx>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=23.2.1
greenlet>=3.0.3
